define('main', ['rd.containers.Panel'], function(){
    // 创建一个RDK的应用
    var app = angular.module("rdk_app", ['rd.containers.Panel']);
    // 创建一个控制器
    app.controller('rdk_ctrl', ['$scope', 'EventService', 'EventTypes',function(scope,EventService, EventTypes){
    	


    }]);
});
