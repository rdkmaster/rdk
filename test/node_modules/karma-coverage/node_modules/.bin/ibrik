#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../ibrik/bin/ibrik" "$@"
  ret=$?
else 
  node  "$basedir/../ibrik/bin/ibrik" "$@"
  ret=$?
fi
exit $ret
