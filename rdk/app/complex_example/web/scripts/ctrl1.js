//有新的下载请追加到数组后面
define(['utils', 'i18n'], function(utils, i18n) {
  window.controller1 = [
        '$scope', 'blockUI',
function( scope,   blockUI) {

    console.log('controller1 is running...')


  }
]});
